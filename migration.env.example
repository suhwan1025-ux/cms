# 데이터 이관 설정 파일
# 이 파일을 복사하여 실제 값으로 수정하세요

# 소스 데이터베이스 (기존 DB) 설정
SOURCE_DB_NAME=contract_management_old
SOURCE_DB_USERNAME=postgres
SOURCE_DB_PASSWORD=meritz123!
SOURCE_DB_HOST=localhost
SOURCE_DB_PORT=5432

# 타겟 데이터베이스 (새 DB) 설정
DB_NAME=contract_management
DB_USERNAME=postgres
DB_PASSWORD=meritz123!
DB_HOST=localhost
DB_PORT=5432

# 이관 옵션
MIGRATION_BATCH_SIZE=1000
MIGRATION_CREATE_BACKUP=true
MIGRATION_CLEAR_TARGET=true 