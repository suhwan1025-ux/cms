# 의존성
node_modules/
package-lock.json

# 환경 변수 (민감한 정보 포함)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.migration

# 로그 파일
logs/**/*.log
logs/**/*.txt
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
!logs/backup/.gitkeep

# 데이터베이스 파일
*.sqlite
*.sqlite3
*.db

# 업로드 파일
uploads/*
!uploads/.gitkeep

# 백업 파일 (데이터 백업만 무시, 스크립트는 포함)
backup/
backup_*/
*.dump
data-export-*.json
complete-export-*.json
schema-*.sql
# 백업 스크립트는 포함
!sql/create_backup_tables.sql
!scripts/backup/

# 이관/업데이트 패키지 (임시 폴더)
CMS_UPDATE_*/
CMS_PERSONNEL_*/
*_이관요약.txt

# 운영체제 파일
.DS_Store
Thumbs.db
desktop.ini

# IDE 설정
.vscode/
.idea/
*.swp
*.swo
*~

# 빌드 출력
build/
dist/
.cache/

# 테스트 커버리지
coverage/

# 임시 파일
tmp/
temp/
*.tmp

# ZIP 파일
*.zip

# 프로덕션 설정 (선택적)
# env.production

node_modules_essential_part*.zip

node_modules.z*
